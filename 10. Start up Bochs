. lib/functions
basename "$0"
export noks=0

touch out
(until grep -q '#' out ; do sleep 5; done ; slowcat ./_1 4 1)| TERM=vt100 bochs -q -f bochsrc >out 2>/dev/null

echo .
cat out
echo .
od -cv out

exit $noks
