. lib/functions
basename "$0"
export noks=0

touch out
(until grep -q '#' out ; do sleep 5; done ; slowcat ./_1 2 .1)| TERM=vt100 bochs -q -f bochsrc 2>/dev/null|tee -a out >/dev/null 2>&1
cp out out_1.txt

exit $noks
