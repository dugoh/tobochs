. lib/functions
basename "$0"
export noks=0

touch out
echo $TERM
(until grep -q '#' out ; do sleep 5; done ; slowcat ./_2 4 1)| TERM=vt100 bochs -q -f bochsrc |tee -a out 

echo .
cat out
echo .
od -cv out

exit $noks
