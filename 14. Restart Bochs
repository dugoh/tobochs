. lib/functions
basename "$0"
export noks=0

touch out
echo $TERM
>out
(until grep -q '#' out ; do sleep 5; done ; sleep 5; slowcat ./_2 2 1)| TERM=vt100 bochs -q -f bochsrc |tee -a out 

echo .
cat out
echo .
od -cv out

exit $noks
